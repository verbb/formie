import{Z as s,e as t,f as a,a0 as o}from"./chunks/framework.DmpHOl9T.js";const u=JSON.parse('{"title":"Spam Protection","description":"","frontmatter":{},"headers":[],"relativePath":"forms/spam-protection.md","filePath":"forms/spam-protection.md","lastUpdated":1782532814000}'),n={name:"forms/spam-protection.md"};function i(r,e,l,d,p,c){return t(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="spam-protection" tabindex="-1">Spam Protection <a class="header-anchor" href="#spam-protection" aria-label="Permalink to &quot;Spam Protection&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>For keyword syntax and examples, see <a href="/guides/configuration/spam-keywords-in-detail">Spam keywords in detail</a>. For how screening layers work together in practice, see <a href="/guides/submissions-workflows/submission-screening-rules-in-practice">Submission screening rules in practice</a>.</p></div><p>Spam protection is about reducing junk submissions without making the form harder for real people to use.</p><p>Formie gives you a few layers to work with, so you can start with lighter filtering and add stronger checks only where they are needed. Most of the control lives in <strong>Formie → Settings → Spam Protection</strong>.</p><p>That page brings together spam handling, keyword rules, built-in submission guards, and captcha provider credentials in one place.</p><p>Captcha failures, keyword matches, submission guard failures, and related signals are evaluated together in the submission workflow’s <strong><code>screen</code></strong> stage. For how that stage is ordered, how it relates to validation, and how to extend it, see <a href="/forms/submission-screening">Submission Screening</a>.</p><h2 id="spam-handling" tabindex="-1">Spam Handling <a class="header-anchor" href="#spam-handling" aria-label="Permalink to &quot;Spam Handling&quot;">​</a></h2><p>At the plugin level, you can choose whether spam submissions should still be saved. Saving them is useful when you need to review false positives, understand what kind of spam is hitting the form, or debug a captcha or filtering rule.</p><p>If you do save spam, you can also set a limit for how many spam submissions Formie should keep before older ones are pruned. That helps keep the database under control without losing all visibility into what is being blocked.</p><p>You can also decide how Formie should respond when a submission is flagged as spam. In many cases, it is better to behave as though the submission was accepted, rather than giving a clear rejection message that helps bots learn what is being blocked. If you prefer, you can instead show an error message.</p><p>If you use email notifications, there is also a plugin setting for whether spam submissions should still trigger them.</p><h2 id="content-rules" tabindex="-1">Content Rules <a class="header-anchor" href="#content-rules" aria-label="Permalink to &quot;Content Rules&quot;">​</a></h2><h3 id="spam-keywords" tabindex="-1">Spam Keywords <a class="header-anchor" href="#spam-keywords" aria-label="Permalink to &quot;Spam Keywords&quot;">​</a></h3><p>Spam keywords are the simplest built-in screening tool. Formie checks the whole submission, and if it matches your keyword definition, the submission will be marked as spam.</p><p>You can match against:</p><ul><li>words or phrases</li><li>combinations such as <code>AND</code>, <code>OR</code>, and <code>NOT</code></li><li>IP addresses or IP ranges</li></ul><p>This makes spam keywords useful for obvious repeat attacks or recurring junk content.</p><h4 id="keyword-definition" tabindex="-1">Keyword Definition <a class="header-anchor" href="#keyword-definition" aria-label="Permalink to &quot;Keyword Definition&quot;">​</a></h4><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Flags content containing the word &quot;spam&quot;. This will not match &quot;spamming&quot; or &quot;Spam&quot;.</span></span>
<span class="line"><span>[match: spam]</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Flags content containing the exact phrase &quot;cheap ham&quot;.</span></span>
<span class="line"><span>[match: cheap ham]</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Flags content only if both &quot;spam&quot; and &quot;bulk&quot; are present.</span></span>
<span class="line"><span>[match: spam AND bulk]</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Flags content if either &quot;spam&quot; or &quot;phishing&quot; is present.</span></span>
<span class="line"><span>[match: spam OR phishing]</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Flags content if it contains either &quot;spam&quot; or &quot;junk&quot; along with &quot;email&quot;.</span></span>
<span class="line"><span>[match: (spam OR junk) AND email]</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Flags content if it does not contain &quot;client&quot;.</span></span>
<span class="line"><span>[match: NOT client]</span></span></code></pre></div><p>You can define each rule on a new line, and you can use parentheses to group logic when needed.</p><h4 id="ip-addresses" tabindex="-1">IP Addresses <a class="header-anchor" href="#ip-addresses" aria-label="Permalink to &quot;IP Addresses&quot;">​</a></h4><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Flags content if the sender&#39;s IP matches. Supports singular, multiple, ranges and CIDR notation.</span></span>
<span class="line"><span>[ip: 192.168.0.1, 192.168.0.2, 192.168.0.3]</span></span>
<span class="line"><span>[ip: 10.0.0.1]</span></span>
<span class="line"><span>[ip: 192.168.0.1-192.168.0.255]</span></span>
<span class="line"><span>[ip: 192.168.0.0/24]</span></span></code></pre></div><h4 id="referencing-other-content" tabindex="-1">Referencing Other Content <a class="header-anchor" href="#referencing-other-content" aria-label="Permalink to &quot;Referencing Other Content&quot;">​</a></h4><p>Spam keywords are stored in the spam settings store (with optional project-config defaults), which means they are not always convenient to edit directly on staging or production when those values are project-scoped. If you want content admins to manage them, or you need them to vary by environment, you can reference another field instead.</p><p>This is commonly done with a Global Set. For example, if you had a Global Set called <code>Forms</code> and a field called <code>Spam Keywords</code>, you could reference it in the Formie spam keywords setting with <code>{forms.spamKeywords}</code>.</p><p>Spam keyword and IP rules are evaluated by <code>SpamHelper</code> during the <code>screen.runSpamChecks</code> workflow task. That includes <code>[match:]</code> logical rules, <code>[ip:]</code> ranges and CIDR notation, and field/global references in keyword lines.</p><h3 id="email-rules" tabindex="-1">Email Rules <a class="header-anchor" href="#email-rules" aria-label="Permalink to &quot;Email Rules&quot;">​</a></h3><p>Global email rules apply to every <strong>Email Address</strong> field on every form during the <strong><code>screen</code></strong> stage. They mark matching submissions as spam rather than showing a field validation error.</p><p>Configure them under <strong>Formie → Settings → Spam Protection → Content Rules → Email Rules</strong>:</p><ul><li><strong>Allowed domains</strong> — one domain or full email address per line (for example <code>company.com</code> or <code>user@company.com</code>). Allowlisted addresses skip global blocked-domain and free-provider checks only; keywords, captchas, guards, and other spam rules still apply.</li><li><strong>Blocked domains</strong> — one domain per line (for example <code>mailinator.com</code>)</li><li><strong>Block free email providers</strong> — rejects addresses from Formie’s maintained free/disposable provider list</li></ul><p>Per-field email settings such as <strong>Blocked Domains</strong>, <strong>Block Free Email Providers</strong>, and <strong>Validate Domain (DNS)</strong> still run separately during field validation. Use global rules when you want the same policy everywhere; use per-field settings when only some forms need stricter email checks.</p><h3 id="text-rules" tabindex="-1">Text Rules <a class="header-anchor" href="#text-rules" aria-label="Permalink to &quot;Text Rules&quot;">​</a></h3><p>Configure these under <strong>Formie → Settings → Spam Protection → Content Rules → Text Rules</strong>:</p><ul><li><strong>Suspicious text detection</strong> — flags keyboard spam, random character strings, and other low-quality filler text in submission fields. Add <strong>Allowed terms</strong> (one per line) for abbreviations or product codes that should not be flagged.</li><li><strong>Maximum links</strong> — marks submissions as spam when the total number of links across all fields exceeds the configured limit.</li></ul><p>Both rules run during the <strong><code>screen.runSpamChecks</code></strong> workflow task, after email rules and before spam keywords.</p><h2 id="submission-throttling" tabindex="-1">Submission Throttling <a class="header-anchor" href="#submission-throttling" aria-label="Permalink to &quot;Submission Throttling&quot;">​</a></h2><p>Submission throttling is <strong>abuse protection</strong>, not entry caps. It limits how quickly forms can be submitted during floods or rapid repeat attempts. Configure it under <strong>Formie → Settings → Spam Protection → Submission Throttling</strong>.</p><p>For contest, registration, or quota-style caps, use per-form <strong><a href="/forms/submission-limits">Submission limits</a></strong> in the form builder instead. Throttling marks excess submissions as spam; submission limits close the form or show a validation error according to your business rules.</p><table tabindex="0"><thead><tr><th>Setting</th><th>What it does</th></tr></thead><tbody><tr><td><strong>Global submission throttling</strong></td><td>Caps the total number of submissions allowed across <strong>all forms</strong> within a time window. Intended for emergency abuse protection during spam floods, not fine-tuning individual forms.</td></tr><tr><td><strong>IP submission throttling</strong></td><td>Requires a minimum <strong>wait time</strong> between submissions from the same IP address on the <strong>same form</strong>. This is not the same as a per-IP <strong>count</strong> limit on the form — see <a href="/forms/submission-limits#ip-wait-time-vs-ip-count-limits">Submission limits</a>.</td></tr></tbody></table><p>Throttling runs during <strong><code>screen.runSubmissionGuards</code></strong>, before honeypot and other passive guards. Failed throttling checks mark the submission as spam and respect your <strong>Spam submission behavior</strong> setting.</p><p>Use conservative values for global throttling. For example, <code>1 per minute</code> across the entire site would block all users after the first submission.</p><h2 id="submission-guards" tabindex="-1">Submission Guards <a class="header-anchor" href="#submission-guards" aria-label="Permalink to &quot;Submission Guards&quot;">​</a></h2><p>Submission guards are built-in passive checks that run <strong>before</strong> captcha integrations and keyword rules.</p><p>Configure them under <strong>Formie → Settings → Spam Protection → Submission Guards</strong>.</p><table tabindex="0"><thead><tr><th>Guard</th><th>What It Does</th></tr></thead><tbody><tr><td><strong>Honeypot</strong></td><td>Renders a hidden field that legitimate users should leave empty. Submissions that fill it in are marked as spam.</td></tr><tr><td><strong>Minimum submit time</strong></td><td>Requires a minimum delay between the form loading and submission.</td></tr><tr><td><strong>Form submit expiration</strong></td><td>Rejects submissions when too much time has passed since the form was first loaded.</td></tr><tr><td><strong>Replay protection</strong></td><td>Prevents duplicate submissions from reusing the same <code>requestToken</code>.</td></tr></tbody></table><p>Honeypot, minimum submit time, and replay protection are enabled by default. Form submit expiration is off by default.</p><h3 id="how-guards-run-in-the-workflow" tabindex="-1">How Guards Run in the Workflow <a class="header-anchor" href="#how-guards-run-in-the-workflow" aria-label="Permalink to &quot;How Guards Run in the Workflow&quot;">​</a></h3><p>Guards are not captcha integrations. They do not appear in the form builder’s captcha picker, and they do not use provider credentials.</p><p>Instead, Formie runs them through dedicated workflow tasks:</p><ol><li><strong><code>screen.runSubmissionGuards</code></strong> — runs during the <strong><code>screen</code></strong> stage, before captcha and keyword checks. If a guard fails, the submission is marked as spam and <code>spamReason</code> is set.</li><li><strong><code>finalize.consumeReplayToken</code></strong> — runs during the <strong><code>finalize</code></strong> stage after a successful, complete submission. Replay protection only consumes the token once the submission has finished processing successfully, so failed or incomplete submissions can retry with the same token.</li></ol><p>Guards run for every final submit. <strong>Universal guards</strong> (global throttling, IP throttling, replay protection, and a required request token) apply to browser, client REST, and GraphQL submissions. <strong>Browser-only guards</strong> (honeypot, minimum submit time, and form submit expiration) run only for traditional form posts that include <code>handle</code> and <code>submitAction</code> in the POST body, because they rely on hidden fields rendered in the browser.</p><p>Client REST and GraphQL submissions must include a <code>requestToken</code> issued by <code>formieClientForm</code>, <code>refreshFormieClientSession</code>, or an equivalent bootstrap call. Drive-by API posts without a token are rejected.</p><p>The honeypot field and <code>formStartedAt</code> timestamp are rendered automatically for browser forms. You do not need to add them manually in templates.</p><h3 id="honeypot-field-name" tabindex="-1">Honeypot Field Name <a class="header-anchor" href="#honeypot-field-name" aria-label="Permalink to &quot;Honeypot Field Name&quot;">​</a></h3><p>The default honeypot input name is <code>formieHoneypot</code>. You can change it under <strong>Honeypot Field Name</strong> if you need to avoid a clash with a real field on your forms.</p><p>If you customise the name, make sure it does not match any field handle or name your forms already use.</p><h3 id="minimum-submit-time" tabindex="-1">Minimum Submit Time <a class="header-anchor" href="#minimum-submit-time" aria-label="Permalink to &quot;Minimum Submit Time&quot;">​</a></h3><p>The browser package records when a form instance was first mounted and sends that value as <code>formStartedAt</code>. Formie compares it against the configured minimum (in seconds) on the server.</p><p>Very fast automated submissions are flagged as spam. Real users who submit immediately after the page loads may also be caught if the minimum is set too high, so tune this value for your forms.</p><h3 id="form-submit-expiration" tabindex="-1">Form Submit Expiration <a class="header-anchor" href="#form-submit-expiration" aria-label="Permalink to &quot;Form Submit Expiration&quot;">​</a></h3><p>Form submit expiration uses the same <code>formStartedAt</code> timestamp as minimum submit time. If the elapsed time exceeds the configured maximum (in seconds), the submission is marked as spam.</p><p>Use this when you want to reject stale form sessions left open for hours or days. It is disabled by default.</p><h3 id="replay-protection" tabindex="-1">Replay Protection <a class="header-anchor" href="#replay-protection" aria-label="Permalink to &quot;Replay Protection&quot;">​</a></h3><p>Replay protection uses the existing per-render <code>requestToken</code> that Formie already issues for browser forms. Formie stores a short-lived cache entry when a token is successfully consumed, and rejects reuse within 24 hours.</p><p>This is separate from save-and-continue draft tokens and static-cache refresh behaviour. It targets repeat POST abuse, not legitimate multi-page navigation.</p><p>For the full screening order and extension points, see <a href="/forms/submission-screening">Submission Screening</a>.</p><h2 id="captchas" tabindex="-1">Captchas <a class="header-anchor" href="#captchas" aria-label="Permalink to &quot;Captchas&quot;">​</a></h2><p>Formie also supports captcha integrations when you need a stronger challenge layer.</p><p>Configure provider credentials under <strong>Formie → Settings → Spam Protection → Captchas</strong>, then enable the providers you need per form in the form builder.</p><p>Use captchas when keyword matching and submission guards are not enough, or when the form is a common attack target. Third-party spam scoring services such as Akismet, CleanTalk, and OOPSpam are configured here too, even though they classify content rather than show a visible challenge.</p><p>See <a href="/integrations/captchas/">Captchas</a> for provider-specific setup guides.</p><h2 id="control-panel-settings" tabindex="-1">Control Panel Settings <a class="header-anchor" href="#control-panel-settings" aria-label="Permalink to &quot;Control Panel Settings&quot;">​</a></h2><p>Spam handling, keyword rules, submission guards, and captcha provider credentials are stored in Formie’s dedicated settings tables rather than <code>config/project</code> plugin settings. That lets privileged admins edit production values when <code>allowAdminChanges</code> is <code>false</code>.</p><p>Project-scoped rows can still deploy through project config. Site-scoped rows remain environment-local.</p>`,74)])])}const h=s(n,[["render",i]]);export{u as __pageData,h as default};
