export const DefaultsSectionIntro = ({ title, description }) => {
    return (
        <div>
            <h3 className="formie-defaults-section-title">{title}</h3>
            <p className="formie-defaults-section-description">{description}</p>
        </div>
    );
};
