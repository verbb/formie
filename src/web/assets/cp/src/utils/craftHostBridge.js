/**
 * @deprecated Prefer `hostBridge: createCraftHostBridge()` on `PluginKitProvider` / `mountFormieReactApp()`.
 */
export { createCraftHostBridge, createCraftHostBridge as configureFormieHostBridge } from '@verbb/plugin-kit-react/utils';
